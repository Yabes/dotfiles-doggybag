- clean: ['~']

- link:
    ~/.zshrc: zsh/zshrc
    ~/.gitconfig: git/gitconfig
    ~/.git_commit_template.txt: git/git_commit_template.txt

- shell:
    ["[ -d ~/.fzf ] || (git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf && y | ~/.fzf/install)", Installing fzf]
